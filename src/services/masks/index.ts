/**
 * Barrels de máscaras de apresentação (AGENTS.md §7).
 * Fonte única de formatação exibível (DRY): moeda e percentual pt-BR.
 */
export { formatCentsAsBRL } from "./money";
export { formatPercent } from "./percent";
